<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rui.guang.infs3634groupassignment">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".WelcomePage"
            android:label="Java OOP APIE Revision App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AppMainPage"
            android:label="Java OOP APIE Revision"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".StartingScreenActivity" />
        <activity android:name=".ScorePage" />
        <activity android:name=".AbstractionVideoActivity" />
        <activity android:name=".PolymorphismVideoActivity" />
        <activity android:name=".InheritanceVideoActivity" />
        <activity android:name=".EncapsulationVideoActivity"></activity>
    </application>

</manifest>